name: MODARYX Browser Accessibility Micro-Proof

on:
  pull_request:
    branches:
      - design/modaryx-premium-hd-20260914-work
    paths:
      - '.github/workflows/modaryx-browser-a11y-microproof.yml'
      - 'qa/check-browser-a11y.mjs'
      - '*.html'
      - 'games/**'
      - 'assets/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  browser-a11y:
    name: Chromium accessibility micro-proof
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false
          fetch-depth: 1
      - name: Locate Chrome
        shell: bash
        run: |
          set -euo pipefail
          CHROME_BIN="$(command -v google-chrome || command -v chromium || command -v chromium-browser || true)"
          test -n "$CHROME_BIN"
          "$CHROME_BIN" --version
          echo "CHROME_BIN=$CHROME_BIN" >> "$GITHUB_ENV"
      - name: Start loopback server
        shell: bash
        run: |
          set -euo pipefail
          python3 -m http.server 4175 --bind 127.0.0.1 >/tmp/modaryx-a11y-http.log 2>&1 &
          echo $! >/tmp/modaryx-a11y-http.pid
          for attempt in {1..40}; do
            curl --fail --silent http://127.0.0.1:4175/index.html >/dev/null && exit 0
            sleep 0.25
          done
          cat /tmp/modaryx-a11y-http.log
          exit 1
      - name: Run browser accessibility micro-proof
        shell: bash
        env:
          MODARYX_TEST_ORIGIN: http://127.0.0.1:4175
        run: |
          set -euo pipefail
          node qa/check-browser-a11y.mjs
      - name: Stop loopback server
        if: always()
        shell: bash
        run: |
          test -f /tmp/modaryx-a11y-http.pid && kill "$(cat /tmp/modaryx-a11y-http.pid)" 2>/dev/null || true
